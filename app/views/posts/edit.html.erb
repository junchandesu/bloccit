<h1>Edit Post</h1>

<div class="row">
	<div class="col-md-4">
			<p>Guidelines for posts</p>
			<ul>
				<li>Make sure the it ryms.</li>
				<li>Do't use the letter "A".</li>
				<li>The incesant use of hashtag will get you banned</li>
			</ul>
	</div>
	<div class="col-md-8">
 		<%= render partial: 'form', locals: {topic: @topic, post: @post} %>
	</div>
</div>


<%= form_for [@topic, @post], multipart: true do |f| %>
    <%= f.hidden_field :picture_cache %>
    <%= f.file_field :picture %>
      
<% end %>