

<div class="row">
  <div class="col-md-8">
   <h1> <p><%= @summary.body %></p> </h1>
  </div>
  <div class="col-md-4">
    <% if policy(@summary).edit? %>
      <%= link_to "Edit", edit_topic_post_summary_path(@topic, @post, @summary), class: 'btn btn-success' %>
    <% end %>
  </div>
</div>

